# 交接說明

## 一句話總結

原本「所有機構分數看起來都一樣」的根因是**五個風險構面中有三個完全沒有資料**
（法遵 30%＋輿情 20%＋評鑑 10%＝60% 權重恆為 0），監督式模型的訓練標籤也全為 0。
現已補齊真實公開資料，分數產生實質變異（標準差 8.11、範圍 3.49~45.59、
1125 所中有 429 個不同分數）。

---

## 一、現在就可以做的事

```powershell
# 產生線上網址用的靜態網站（已驗證可用，10.5 MB）
python scripts/export_static.py
python -m http.server 8000 --directory docs   # 本機預覽 http://127.0.0.1:8000/

# 產生給評審的單一執行檔
python scripts/build_exe.py                   # 產出 release/
```

GitHub Pages 的完整設定步驟見 `README_部署.md`。

---

## 二、還沒完成的一件事：輿情覆蓋率

目前 `posts.csv` 只有 114 則、涵蓋 29 所機構（覆蓋率 2.6%），
因為採集途中 Google News 對本機 IP 啟動限速（速率從每分鐘 26 所掉到 1 所）。

**採集程式本身沒問題**，已抓到的資料品質很好，例如：

- 「新北市私立吉尼爾幼兒園不當對待案件說明 教育局：6人遭處分、最重終身列管」
- 「童遭拍打拖行、持剪威嚇 新莊吉尼爾幼兒園被重罰39萬發聲道歉了」
- 「15幼童塞計程車返園惹議！幼兒園挨罰9萬 家長也吃罰單」

這些都正確對應到機構代碼。

### 限速解除後執行（建議隔數小時或換網路／行動熱點）

```powershell
python scripts/collect_sentiment.py --delay 1.5
python scripts/build_dataset.py
python scripts/export_static.py
```

先用 `--city-only` 試跑（只 21 個查詢）確認限速已解除，再跑完整版：

```powershell
python scripts/collect_sentiment.py --delay 1.5 --city-only
```

### 已知的資料損失（已加防護，需重跑補回）

輿情資料被清空了兩次，成因不同，兩處都已修正：

**第一次：388 則 → 114 則**
整合腳本 `build_dataset.py` 會**就地覆寫**採集結果，而 `relink()` 會跳過
沒有機構名稱的列——市級背景報導（未指名園所者）因此被刪除。
修正：`relink(..., keep_unattributed=True)` 讓輿情表保留未指名紀錄
（它們不計入任何機構分數，但構成輿情頁的整體聲量與關鍵詞基礎）。

**第二次：114 則 → 0 則**
`collect_sentiment.py --city-only` 執行時全部查詢都被限速，
而 `fetch_news()` 捕捉例外後回傳空陣列，於是整趟採集「成功結束」卻一則都沒抓到，
腳本就把 0 筆結果寫入覆蓋掉原有資料。

同一類錯誤出現兩次，因此**兩個腳本都加上寫檔防護**：

| 腳本 | 防護 |
|---|---|
| `build_dataset.py` | `write_back()`：對應後筆數減少就拒絕覆寫 |
| `collect_sentiment.py` | 新結果少於既有檔案就拒絕覆寫，並提示可能是限速；確定要取代須加 `--force` |

重跑採集即可補回。教訓：**採集腳本必須把「抓不到」與「確實沒有」分開處理**，
前者不該被當成有效結果寫入。

### 已針對限速做的調整

| 調整 | 說明 |
|---|---|
| 策略改為「議題掃描為主」 | 用 21 個風險關鍵詞掃全市，而非逐一查詢 1121 所。新聞會報導的是「出事的園所」，逐一查詢等於用上千次請求換取幾乎全空的結果 |
| 逐機構深查限縮範圍 | 預設只查**有裁罰紀錄**的機構（184 所），這些才是最可能有後續報導的對象 |
| 指數退避 + 永久調高延遲 | 遇 429 時不只退避單次請求，而是把後續延遲一併調高；否則每一筆都會再撞一次限速 |
| 行政區掃描改為選用 | `--with-districts` 會讓查詢數從 21 增到 108，容易整批卡住 |

其他參數：`--all-institutions`（全查，很慢）、`--limit N`、`--with-ptt`、`--city-only`。

---

## 三、本次修正的重要問題

### 資料缺口（核心問題）

| 資料表 | 修正前 | 修正後 |
|---|---|---|
| penalties | 不存在 | 354 筆，涵蓋 183 所（16.3%） |
| evaluations | 不存在 | 1188 筆，涵蓋 1074 所（95.5%） |
| posts | 不存在 | 114 則（待補齊） |
| ledger | 不存在 | 2541 筆科目明細 |
| institutions | 29 所（非營利＋公校） | **1125 所**（全新北市教保機構） |

母體從 29 所擴到 1125 所是關鍵：裁罰紀錄幾乎全集中在**私立**園，
而原本的母體只有非營利園與公校，兩者幾乎不重疊——即使抓到裁罰也對不上任何機構。

### 評鑑判讀偽陽性（影響 95% 機構）

`features.py` 原本以字典查表判斷評鑑結果：

```python
EVAL_RESULT_PENALTY = {"不通過": 100.0, "追蹤輔導": 55.0, "通過": 0.0}
f["eval_penalty"] = EVAL_RESULT_PENALTY.get(str(result), 30.0)   # ← 預設 30 分
```

但全國教保資訊網的評鑑結果是**描述性字句**，實際只有三種字樣：

```
基礎評鑑－全數指標通過
基礎評鑑－部分指標通過
追蹤評鑑－全數指標通過
```

這些鍵一次都不會命中，於是每一所有評鑑紀錄的機構都落到預設值 30 分，
**連「全數指標通過」的園所也被扣分**。同樣的錯誤也出現在
`scoring.explain()` 的 `result != "通過"` 比對上，導致風險報告出現
「最近一次基礎評鑑**未完全通過**：結果為『追蹤評鑑－**全數指標通過**』」
這種自相矛盾的敘述。

已改為 `config.eval_result_level()` 統一判讀，三處共用。判讀時特別注意
「部分指標通過」字面含「通過」、實質是未全數通過，必須判為缺失；
無法判讀時回傳 unknown 並以缺值處理，不預設當成有缺失。

修正效果：

| 指標 | 修正前 | 修正後 |
|---|---|---|
| 評鑑風險非零占比 | 95.5% | **10.8%** |
| 評鑑風險平均 | 25.27 | 1.69 |
| `rule_only` AUC | 0.5739 | **0.5825** |
| 綜合分數標準差 | 8.41 | **9.04** |

移除雜訊後模型的鑑別力反而提升，這正說明原本那 95% 的評鑑扣分是純噪音。

### 機構分類（你提出的疑慮）

`institutions.csv` 本身沒有混入國中小，但查證時發現三個真實錯誤：

1. **`extract_public_schools.py` 會抓國小／國中／高中本體**並標為「公立國小／國中／高中」。
   已限縮為只抓市立幼兒園（單位代號 136xx）。
   關鍵理由：國小附設幼兒園**沒有獨立決算**，財務併入學校預算；若拿學校總收支
   當附設幼兒園的財務，數量級會差好幾倍，所有財務比率與鑑識檢定都會失真。
2. **8 所「職場互助教保服務中心」被誤判為非教保機構**（台電林口發電廠、
   中華郵政樹林三多郵局、國防部陸軍、交通部航港局等附設）。依幼兒教育及照顧法
   第 10 條，這是與幼兒園並列的法定教保服務機構類型，應納入監理。
3. `config.ORG_TYPES` 移除學校類型，只留 公立／私立／非營利／準公共。

新增 `inst_kind` 欄位讓分類可檢視：

```
獨立幼兒園              848
國小附設幼兒園          196
法人公司附設幼兒園       33
國中附設幼兒園           31
高中職大專附設幼兒園       9
職場互助教保服務中心       8
```

### 財務資料掛載失敗

官方名稱帶委辦註記「新北市安溪非營利幼兒園**(委託社團法人桃園市教保服務人員協會辦理)**」，
決算報告只寫「新北市安溪非營利幼兒園」，正規化後完全不同 → 財務資料掛不上。
已在 `textnorm.canonical_name()` 剝除該註記，**對應率從 4/38 提升到 34/38**。

### OCR 萃取品質

決算 PDF 的文字層損毀必須 OCR，而 OCR 有系統性誤字：
教保費收入→教**係**費收入、業務費→**緒**務費、修繕購置費→**值繒**購置費、
呆帳損失→**紙**帳損失。原程式用**精確字串**比對科目名稱，所以 `revenue_tuition` 整欄空白。

修正後：
- 模糊科目比對（尾字必須相符，避免「…收入」誤認為「…支出」）
- 章節感知（「支 出 ﹌」帶雜訊字元會讓段落不切換，導致 12 個支出科目全落空）
- **決算數取值依欄位數判斷**：預算欄為「-」時只有 [決算, 增減] 兩欄，
  固定取索引 1 會拿到被 OCR 掉字的重複值（實測「其他收入」拿到 675,731 而非 1,675,731）
- **會計恆等式三檢核**（收入明細=合計、支出明細=合計、收支差=餘絀），
  未通過時自動以更高解析度重試；仍失敗則標記 `verified=0` 而非留下錯誤數字
- 效果：單一機構從 8 個科目提升到 19~20 個，恆等式差額多為 0

### 機構代碼碰撞

`stable_inst_id()` 原本用剝除設立別後的名稱雜湊，導致
「新北市**立**三芝幼兒園」與「財團法人…附設新北市**私立**三芝幼兒園」共用同一代碼。
已改用完整名稱雜湊，並在採集與整合階段都加入碰撞偵測。

### exe 打包未含資料

原本 spec 只打包 `web/`，評審端打開會是空的。已修正並加入
`scripts/build_exe.py`，會**實際啟動 exe 並查詢分析結果**才算通過——
打包最常見的失敗不是建置失敗，而是建置成功但執行期缺資源。

---

## 四、目前的資料覆蓋率與其意義

| 構面 | 設定權重 | 覆蓋率 | 標準差 | 說明 |
|---|---|---|---|---|
| 法遵 | 30% | 16.3% | 11.29 | 真實裁罰紀錄，鑑別力最強 |
| 財務 | 25% | 3.4% | 2.24 | 僅非營利園有公開決算 |
| 營運 | 15% | 0% | 0 | **已排除**（見下） |
| 評鑑 | 10% | 95.5% | 6.13 | 覆蓋最完整 |
| 輿情 | 20% | 2.6% | 4.14 | 待限速解除後重跑 |

綜合分數：平均 22.16、標準差 8.41、範圍 3.49~49.27、1125 所中有 432 個不同分數。

模型：訓練樣本 1125、正樣本 50 所、AUC 0.576、預警提前平均 157 天。

**「覆蓋率低」與「分數低」是兩件事。** 系統以 `scoring.rule_score()` 的
構面可得性機制處理：整份資料集沒有某類來源時，該構面會從加權平均中排除
並重新正規化其餘權重，而不是以 0 分計入——後者會系統性壓低所有機構分數，
把真正有訊號的構面稀釋掉。

### 營運構面已排除（已驗證）

官方主檔對每一所都有核定人數，但缺**實際招收人數**與**教保人員數**，
因此超收率與師生比全都算不出來。修正前該構面對所有機構一律給 0 分卻照
15% 權重計入，等於把「查不到資料」當成「營運無虞」，還稀釋了真正有訊號的構面。

修正時踩到一個陷阱：不能用 `features[k] is not None` 判斷，因為 features 對
算不出來的指標會給 `0.0`（例如 `over_enroll=0.0`、`principal_multi=0`）而非
`None`。已改為檢查來源欄位 `enrolled` / `teacher_count` / `turnover` 是否存在。

修正後實際效果（以風險第 1 名為例）：

```
營運風險  available=False  weight=0.0     ← 排除，不再稀釋
法遵風險  weight 0.30 → 0.3529           ← 其餘權重重新正規化
財務風險  weight 0.25 → 0.2941
評鑑風險  weight 0.10 → 0.1176
輿情風險  weight 0.20 → 0.2353
綜合分數  45.59 → 49.27
```

要讓營運構面生效，需補齊各園的實際招收人數與**分齡**教保人員數
（幼照法第 16 條的兩歲班 8:1 與三至五歲班 15:1 須分別計算，
不可用全園平均互相稀釋）。目前僅 34 所非營利園從決算報告附註取得這些數字。

---

## 五、AI agent

`app/ai_agent.py` 是 Claude 工具呼叫迴圈，共 11 個工具。本次新增三個：

- `search_live_news` — **即時上網查證**。裁罰公告有時間差，新聞常先揭露事件，
  這是本代理人與純特徵查詢的關鍵差異
- `compare_with_peers` — 列出具體同儕機構及其指標，判斷是同業普遍現象還是個別異常
- `get_data_coverage` — 回報各構面是否真有資料。系統提示要求代理人**下結論前先確認**，
  避免把「查無裁罰紀錄」誤讀為「已查核確認合規」

未設定 API 金鑰時走 `rule_based_review()`，回傳結構相同但
`engine` 欄位標記為 `"rules"`，前端據此顯示「規則式深度檢視（未使用語言模型）」，
不會把規則結果包裝成 AI 推論。

金鑰在儀表板設定頁輸入，存於 `settings.json`；靜態網站版不含此功能。

---

## 六、完整重建流程

```powershell
# 1. 採集公開資料（需網路）
python scripts/collect_ece.py                    # 機構主檔、裁罰、評鑑（約 40 分）
python scripts/collect_sentiment.py --delay 1.5  # 新聞輿情

# 2. 從決算 PDF 擷取財務（需 Tesseract，約 1.5 小時）
python scripts/extract_nonprofit_financials.py
python scripts/extract_public_schools.py

# 3. 整合
python scripts/build_dataset.py

# 4. 交付
python scripts/export_static.py     # → docs/（GitHub Pages）
python scripts/build_exe.py         # → release/（執行檔）
```

環境需求與環境變數見 `README_部署.md`。

---

## 七、驗證用的檢查腳本

`.tmp/` 下的腳本可重複執行以確認狀態：

| 腳本 | 用途 |
|---|---|
| `final_check.py` | 機構型態、構面變異、模型指標、靜態網站完整性 |
| `test_textnorm.py` | 名稱正規化與代碼唯一性（34 項） |
| `test_fuzzy.py` | OCR 模糊科目比對（33 項） |
| `test_settled.py` | 決算數取值規則（19 項） |
| `verify_engine.py` | 端到端跑引擎並輸出分數分布 |

註：`.tmp/` 已列入 `.gitignore`，不會提交到儲存庫。
